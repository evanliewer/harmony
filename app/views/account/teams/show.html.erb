<%= render 'account/shared/page' do |p| %>
  <% p.content_for :title, t('.header', teams_possessive: possessive_string(@team.name)) %>

  <% p.content_for :body do %>
    <%= render 'account/demographics/index', demographics: @team.demographics, hide_back: true %>
    <%= render 'account/departments/index', departments: @team.departments, hide_back: true %>
    <%= render 'account/locations/index', locations: @team.locations, hide_back: true %>
    <%= render 'account/items/index', items: @team.items, hide_back: true %>
    <%= render 'account/organizations/index', organizations: @team.organizations, hide_back: true %>
    <%= render 'account/retreats/index', retreats: @team.retreats, hide_back: true %>
    <%= render 'account/reservations/index', reservations: @team.reservations, hide_back: true %>
    <%= render 'account/items/tags/index', tags: @team.items_tags, hide_back: true %>
    <%= render 'account/flights/index', flights: @team.flights, hide_back: true %>
    <%= render 'account/flights/timeframes/index', timeframes: @team.flights_timeframes, hide_back: true %>
    <%= render 'account/flights/checks/index', checks: @team.flights_checks, hide_back: true %>
    <%= render 'account/organizations/contacts/index', contacts: @team.organizations_contacts, hide_back: true %>
    <%= render 'account/notifications/index', notifications: @team.notifications, hide_back: true %>
    <%= render 'account/notifications/flags/index', flags: @team.notifications_flags, hide_back: true %>
    <%= render 'account/notifications/requests/index', requests: @team.notifications_requests, hide_back: true %>
    <%# 🚅 super scaffolding will insert new children above this line. %>
    
    <%= render 'account/scaffolding/absolutely_abstract/creative_concepts/index', creative_concepts: @team.scaffolding_absolutely_abstract_creative_concepts, hide_back: true unless scaffolding_things_disabled? %>
  <% end %>
<% end %>
