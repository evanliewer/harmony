<% if can? :read, Game.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :games]),
    label: t('games.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('games.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
