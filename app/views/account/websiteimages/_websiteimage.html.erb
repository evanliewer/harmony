<% team = @team || @team %>
<% context ||= team %>
<% collection ||= :websiteimages %>
<% hide_actions ||= false %>
<% hide_back ||= false %>

<% with_attribute_settings object: websiteimage do %>
  <tr data-id="<%= websiteimage.id %>">
    <%= render "shared/tables/checkbox", object: websiteimage %>
    <td><%= render 'shared/attributes/text', attribute: :name, url: [:account, websiteimage] %></td>
    <td><%= render 'shared/attributes/text', attribute: :description %></td>
    <td><%= render 'shared/attributes/image', attribute: :image %></td>
    <%# 🚅 super scaffolding will insert new fields above this line. %>
    <td><%= render 'shared/attributes/date_and_time', attribute: :created_at %></td>
    <td class="buttons">
      <% unless hide_actions %>
        <% if can? :edit, websiteimage %>
          <%= link_to t('.buttons.shorthand.edit'), [:edit, :account, websiteimage], class: 'button-secondary button-smaller' %>
        <% end %>
        <% if can? :destroy, websiteimage %>
          <%= button_to t('.buttons.shorthand.destroy'), [:account, websiteimage], method: :delete, data: { confirm: t('.buttons.confirmations.destroy', model_locales(websiteimage)) }, class: 'button-secondary button-smaller' %>
        <% end %>
        <%# 🚅 super scaffolding will insert new action model buttons above this line. %>
      <% end %>
    </td>
  </tr>
<% end %>
