<% if can? :read, Season.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :seasons]),
    label: t('seasons.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('seasons.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
