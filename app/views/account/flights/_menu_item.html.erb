<% if can? :read, Flight.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :flights]),
    label: t('flights.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('flights.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
