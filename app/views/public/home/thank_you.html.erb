<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Thank You!</title>
  <style>
    /* Reset default margin and padding */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* Background styling */
    body {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      background-image: url('https://wallpapers.com/images/high/new-zealand-1920-x-1200-background-i7g1261wjsfn7hi7.webp'); /* Replace with your image URL */
      background-size: cover;
      background-position: center;
      font-family: Arial, sans-serif;
      color: #333333;
    }

    /* Overlay for a slight darkening effect */
    body::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.5); /* Dark overlay */
      z-index: 0;
    }

    /* Centered thank you message box */
    .thank-you-container {
      position: relative;
      z-index: 1;
      text-align: center;
      background-color: #0076ff; /* Blue background for the box */
      color: #ffffff; /* White text */
      padding: 40px 20px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow for depth */
      max-width: 400px;
    }

    /* Headline styling */
    h1 {
      font-size: 2.5em;
      margin-bottom: 10px;
    }

    /* Paragraph styling */
    p {
      font-size: 1.2em;
      margin-bottom: 20px;
    }

    /* Button styling */
    .back-home-button {
      padding: 10px 20px;
      font-size: 1em;
      color: #0076ff;
      background-color: #ffffff;
      border: none;
      border-radius: 5px;
      text-decoration: none;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .back-home-button:hover {
      background-color: #e0e0e0;
    }
  </style>
</head>
<body>

  <!-- Thank you message container -->
  <div class="thank-you-container">
    <h1>Thank You!</h1>
    <p>Your form has been successfully submitted. We appreciate your time and effort in filling it out.</p>
    <%= link_to "Submit Again", waiver_path(retreat: params[:retreat]), class: 'back-home-button' %>
 
  </div>

</body>
</html>
